# Volunteer & Donation Platform (Client)

## Setup
```bash
cd client
npm i
npm run dev
```

### Configure API base (optional)
Create a `.env` in `client/`:
```bash
VITE_API_BASE=http://localhost:8080
```
